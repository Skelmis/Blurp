{% extends "base.html.jinja" %}

{% block content %}
    <div class="hero">
        <h1>{{ title }}</h1>
    </div>
    <div class="content">
        <section>
        <b>Type:</b> {{ request.type }}<br>
        <b>URL:</b> {{ request.url }}{% if request.query_params %}?{{ request.query_params }}{% endif %}<br>
        <b>Request made at:</b><br>
        <ul>
            <li>{{ made_at_time }}</li>
            <li>{{ request.made_at.strftime("%-I:%M:%S %p, %d-%m-%Y %Z") }}</li>
        </ul>
        <b>Headers:</b>
        <ul>
            {% for k, v in headers.items() %}
            	<li><b>{{ k }}</b>: {{ v }}</li>
            {% endfor %}

        </ul><br>
        {% if request.body %}
        <b>Body:</b><br>{{ request.body }}
        {% else %}
        <b>No request body.</b>
        {% endif %}
        </section>
        <section>
            <a href="/">Go home</a>
        </section>
    </div>
{% endblock content %}
